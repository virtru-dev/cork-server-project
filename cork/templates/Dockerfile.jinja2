# Generated by cork. Do not commit
FROM {{ docker_repo }}:{{ docker_tag }}

{% if has_cork_server_setup %}
RUN mkdir -p /cork-setup
COPY setup /cork-setup

{% for setup_file in setup_files %}
{% if loop.first %}RUN {% else %}    {% endif %}{{ setup_file }} && \
{%- endfor %}
    rm -rf /cork-setup

{% endif %}

COPY cork /cork